#include <stdio.h>
#include "utils.h"
#include "cli.h"

int main(int argc, char **argv) {
    // Parse and Work on bin arguments
    (void) argc;
    (void) argv;

    // Start CLI
    start_cli();

    return 0;
}
